# deproc

`deproc` is a language-agnostic code processing framework. It provides a modular plugin architecture for language-specific implementations, allowing developers to extract symbols, build symbol tables, resolve dependencies, and link code entities across multiple programming languages through a unified interface.